TODO

Enable adding words to a "deck"
  -click on it, add it to the backend, figure out some way of getting out to the user
    -could just give the user a list that allows them to import it in to the deck -> need some way of making it allow to add pictures, but that's a future problem
We could work on the ability to import those automatically to an Anki deck - not sure how this would screw with the metadata
